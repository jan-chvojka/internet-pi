# {{ ansible_managed }}
---
version: "3"

services:
  influxdb:
    container_name: node-red
    image: nodered/node-red:2.2.3-12
    ports:
      - "1880:1880"
    volumes:
      - './node-red/:/data'
    restart: unless-stopped
